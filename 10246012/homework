# 作業 003

* 請寫一段 Ruby 程式，把 `[1, 3, 5, 7, 9]` 變成 `[2, 4, 6, 8, 10]`
```
a=[1,3,5,7,9]

p a.map{|sum|sum+1}
```
* 請印出在 1 到 2000 中，可以被 4 整除，但不能 100 整除，但又要可以被 400 整除的數字。
```
c = (1..2000).to_a 

p c.select{|x| x%4==0 && x%100!=0 || x%400==0} 
```
* 請印出在 1 到 100 中所有單數的總和。
```
e=(1..100).to_a
f=e.select{|x|x.odd?}
p f.inject(:+)
```
* 請試著解釋什麼是 Symbol。
Symbols如同一個識別符號 一個Symbol會指向同一個結果
## 注意事項
請發送 PR 到 `homework/2016-03-24` branch!

## 繳交期限：2016/3/30 23:59 前
