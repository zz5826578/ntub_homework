#作業 003
*請寫一段 Ruby 程式，把 [1, 3, 5, 7, 9] 變成 [2, 4, 6, 8, 10]
```
a=[1, 3, 5, 7, 9]
p a.to_a.map{|ball| ball+1}
```
*請印出在 1 到 2000 中，可以被 4 整除，但不能 100 整除，但又要可以被 400 整除的數字。(潤年計算公式)
```

 (1..2000).to_a.each { |a|
 if( a%4==0 && a%100!=0 or a%400==0) 
 		p a
 		 end
 		}
```
*請印出在 1 到 100 中所有單數的總和。
aa= (1..100).to_a
p a.select { |num| num.odd? }.inject{|sum,b| sum+b}
*請試著解釋什麼是 Symbol。
Symbol是唯一且不會變動的識別名稱，用冒號開頭，而且相同名稱的Symbol不會再重複建構物件，所以使用Symbol可以執行的更有效率。
